// avance de hoy viernes 12 de junio

function setup() {
  createCanvas(400, 400);
  textSize(30);
  textAlign(CENTER,CENTER);
}

function draw() {
  background(220);
  textSize(frameCount);
  text("esto he avanzado", width/2,height/2)

}


// avance de hoy domingo 14 de junio 

function setup() {
  createCanvas(400, 400);
  textSize(30);
  textAlign(CENTER,CENTER);
}

function draw() {
  background(220);
  textSize(frameCount);
  text("esto he avanzado", width/2,height/2)

}
